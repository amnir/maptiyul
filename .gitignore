.DS_Store
__pycache__/
.playwright-mcp/
# Supabase function secrets — only .env.example (names only) is committed
supabase/functions/**/.env
supabase/.env
.env
# Large, regenerable scrape intermediates (rebuild with scrape/*.py)
data/posts.json
data/urls.txt
